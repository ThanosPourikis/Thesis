	<h1>Price API</h1>
			<div class="card">
			<div class="card-body">
				<h4 class="card-title">Datasets</h4>
			</div>
			<div class="card-body">
				{% for i in datasets %}
					<a href="/prices_api/{{i}}" class="card-link">{{i}}</a>
				{% endfor %}
				</div>
			</div>
	<h1>Metrics API</h1>
	{% for i in datasets %}
			<div class="card">
			<div class="card-body">
				<h4 class="card-title">{{i}}</h4>
			</div>
			<div class="card-body">
					{% for j in models %}
						<a href="/{{i}}/metrics_api/{{j}}" class="card-link">{{j}}</a>
					{% endfor %}
				</div>
			</div>
	{% endfor %}

