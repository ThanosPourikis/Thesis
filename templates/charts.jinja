{% extends 'base.jinja' %}

{% block content %}
	<h1>{{title}}</h1>
	{% for i in df.columns %}
	<div id='{{i}}'>
	<script>
		var graph = {{ get_json(df,y,i) | safe }};
		Plotly.plot('{{i}}', graph, {});
	</script>
</div>
{% endfor %}
{% endblock content %}




